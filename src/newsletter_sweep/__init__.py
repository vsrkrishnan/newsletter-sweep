"""newsletter-sweep: an always-on agent that triages newsletters/feeds
into a knowledge base you already own.

Provider-agnostic (Anthropic, OpenAI, Ollama), source-agnostic (RSS, IMAP),
sink-agnostic (local markdown, Notion). Runs headless — cron, GitHub Actions,
or by hand.
"""

__version__ = "0.1.0"
